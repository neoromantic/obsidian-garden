```dataview
table without id 
	(![](" + poster + ")) as Постер,
	file.link as Название,
	string(year) as Год, 
	"by " + director as Режиссёр,
	"⭐ " + scoreImdb as "⭐ IMDB"
from #фильм  
where poster != null 
where !this.view-only or contains(tags, this.view-only)
```